/* images */
import Logo from "../../pages/images/logo.svg"

export default function Header() {
    return (
        <header> 
            <img src={Logo} alt="Logo marcar camp." /> 
        </header>
    )
}